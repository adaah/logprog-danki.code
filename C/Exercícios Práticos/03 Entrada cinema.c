#include<stdio.h>
#include<stdlib.h> 

int main() 
{ 
    int horaEntrada = 20;
    int horaAtual = 20;

    if (horaEntrada >= horaAtual) {
        printf("Pode entrar, aproveite o filme!");
    } else {
        printf("Opa, você não pode entrar!");
    }
  
  
    return 0;
} 